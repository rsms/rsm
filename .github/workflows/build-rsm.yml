name: Build rsm

on:
  push:
    branches: ["main"]
    paths:
      - "src/**"
      - build.sh
    tags:
      - "v*"
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-linux-musl
          - i386-linux-musl
          # - aarch64-linux-musl
          # - riscv64-linux-musl
          # #- x86_64-windows-gnu

    name: ${{ matrix.target }}

    env:
      prerelease: false

    steps:
      - name: Checkout Source
        uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Install ninja
        run: sudo apt-get install -y ninja-build

      - name: Install zig
        run: |
          curl -L https://ziglang.org/download/0.9.1/zig-linux-x86_64-0.9.1.tar.xz | tar -xJ

      - name: Build rsm-${{ matrix.target }}
        run: |
          CFLAGS_HOST="-target ${{ matrix.target }}" \
          LDFLAGS_HOST="-target ${{ matrix.target }}" \
          CC="$PWD/zig-linux-x86_64-0.9.1/zig cc" \
          bash build.sh -static -out=out-${{ matrix.target }} "$@"

      - name: Define version (tag)
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION=${{ github.ref }}
          VERSION=${VERSION:11}  # refs/tags/v1.2.3 => 1.2.3
          echo "rsm_version=$VERSION" >> $GITHUB_ENV

      - name: Define version (branch)
        if: ${{ ! startsWith(github.ref, 'refs/tags/v') }}
        run: echo "rsm_version=${{ github.sha }}" >> $GITHUB_ENV

      - name: Create archive
        run: |
          VARIANT=$(echo "${{ matrix.target }}" | cut -d- -f1 -f2)
          NAME=rsm-${{ env.rsm_version }}-$VARIANT
          ARCHIVE=$NAME.tar.gz
          echo "rsm_archive=$ARCHIVE" >> $GITHUB_ENV
          mkdir $NAME
          mv out-${{ matrix.target }}/rsm $NAME/
          tar -czvf "$ARCHIVE" "$NAME"

      - name: Upload archive (unless tag)
        if: ${{ ! startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v3
        with:
          name: "${{ env.rsm_archive }}"
          path: "${{ env.rsm_archive }}"
          retention-days: 1

      - name: Publish release (if tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: ncipollo/release-action@v1.8.10
        with:
          prerelease: false
          artifacts: "${{ env.rsm_archive }}"
          replacesArtifacts: true
          tag: "v${{ env.rsm_version }}"
          name: "rsm ${{ env.rsm_version }} "
          body: "rsm ${{ env.rsm_version }} built from ${{ github.sha }}"
          allowUpdates: true
          token: ${{ secrets.GITHUB_TOKEN }}
